# Flucoma microservice requirements
-r /app/requirements/microservices.txt

# FluCoMa-specific dependencies
# (Note: FluCoMa CLI is installed via Docker, not pip)

# Audio processing utilities
scipy>=1.7.0
numpy>=1.21.0

# AWS S3 support
boto3>=1.20.0
